{% extends 'layouts/default.twig' %}

{% block content %}

	<h1>{{ title }}</h1>

	<div class="posts">

		{% for post in posts %}

			<div class="post">
				<h2>{{ post.title }}</h2>
				<p>{{ post.body }}</p>
			</div>

		{% endfor %}

	</div>

{% endblock %}
